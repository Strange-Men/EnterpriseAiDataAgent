# 版本记录 — Enterprise AI Data Agent

## v0.9.6 — Crash Hardening: Null Guards, AbortController Cleanup, Unmount Safety (2026-06-02)

### 修复 6 个异常路径崩溃点

#### 🔴 High
- `ai-analysis-panel.tsx`: AbortController cleanup — ref 被捕获为 null 导致 abort/clearInterval 不执行。修复：cleanup 内直接读取 ref
- `ai-analysis-panel.tsx`: runAnomaliesMode — `res.anomalies`、`res.summary`、`res.interpretations`、`res.recommended_actions` 共 5 处无 null guard → 白屏。修复：可选链 + 默认值

#### 🟡 Medium
- `ai-analysis-panel.tsx`: buildProfileMd / runFullAnalysisMode — `res.profile` 无 null guard → 白屏。修复：参数类型扩展 + 入口 guard + fallback
- `ai-analysis-panel.tsx`: runAutonomousMode — `profileRes.profile.columns` 无 guard → 白屏。修复：提前 return
- `api.ts`: `res.body!.getReader()` 非空断言 — 代理/CDN 返回空 body → 白屏。修复：null check + onError 回调
- `ai-analysis-panel.tsx`: `aiEvaluate` fire-and-forget — 无 unmount 守卫 → React 警告 + 内存泄漏。修复：`mountedRef` 检查

#### 🟢 Low
- `investigation-workspace.tsx`: SSE `onError`/`onDone` 回调 — 无 unmount 守卫 → setState on unmounted component。修复：`mountedRef` 检查

## v0.9.5 — ESLint 清零, Test Isolation & Code Quality (2026-06-02)

### ESLint 39→0 清零
- no-unused-vars (22处)：删除未使用的 import/变量，未使用参数加 `_` 前缀
- no-explicit-any (10处)：`any` → `unknown` / `Record<string, unknown>` / 具体类型
- react-hooks/exhaustive-deps (5处)：补全缺失的依赖项，ref 副本防护
- no-console：`console.log` → `console.info`

### 测试隔离
- ISSUE-014: `QueryHistory` 新增 `use_memory=True` 参数，跳过 DuckDB 持久化
- 测试中使用 `use_memory=True` 隔离，避免共享 DB 状态

### Bug 修复
- `test_token_budget.py`: 修正 CJK token 估算测试断言（v0.9.4 heuristic 变更后未同步）
- `runFullAnalysisMode`: `suggestedQuestions` 现在正确返回（之前赋值后未返回）

### 代码质量
- `VirtualDataTable.tsx`: 移除显式 `any` 类型，CSV 解析使用 `Partial<SalesRow>`
- `logger.ts`: `console.log` → `console.info` 符合 no-console 规则
- `ai-analysis-panel.tsx`: effect cleanup 中 ref 副本防护
- `follow-up-input.tsx`: `setQuestion` 包装为 `useCallback` 避免每次渲染创建新引用

## v0.9.4 — Security Hardening, Bug Fixes & Code Quality (2026-06-02)

### 安全加固 (P0)
- SQL 注入防护：只读执行器 + `sql_validator.py` 拒绝非 SELECT 语句
- Table name 注入防护：正则验证 `[a-zA-Z0-9_]`
- Error 消息 sanitized：不泄露内部信息给客户端
- 全局异常处理器：正确处理 HTTPException(404→404)、ValidationError(422)、通用 Exception(500)

### 关键 Bug 修复 (P0)
- Query 取消真正生效：`threading.Event` 替代 `dict[bool]`
- Anthropic Client 单例缓存：避免每次调用重建 client
- `ai_analyze_multi` 不再阻塞事件循环：`run_in_executor` + 180s 超时
- Streaming 不再静默挂起：stream 结束无 "done" 事件时正确处理
- SSE 直连 URL 环境变量化：`NEXT_PUBLIC_API_URL`
- Guardrail 支持步骤级超时检查：`check_during_step()`

### 重要 Bug 修复 (P1)
- Token 预估 CJK 修正：CJK ~1.5 char/token，英文 ~3 char/token
- Token truncation O(n²) → O(n log n)：二分搜索替代逐行删除
- `raise last_error` 防御：检查 None
- Anomaly Detection JSON 解析 fallback：`_parse_llm_json()` 支持 markdown-wrapped JSON
- Streaming 复用 retry helper：`on_retry` 回调
- Observability middleware header 时序修复
- Monaco Schema Cache 5 分钟 TTL + `invalidateSchemaCache()`
- Monaco Completion Provider 单次注册 + unmount 清理
- Keyboard Listener 去抖：useRef 替代依赖 currentSql

### 前端代码质量 (P2)
- Dashboard i18n 补全：20+ 硬编码字符串改用 t()
- Settings Page i18n 补全
- Dashboard 重复 selector 清理
- Error Boundary 全局覆盖（shell layout 顶层）

### 后端健壮性 (P3)
- Thread-safe Singleton：`threading.Lock` 保护 lazy init
- Export 内存优化：`df.to_csv()` 替代 `iterrows()`
- Route Prefix 统一：添加 `/tables/{name}/rename` 和 `/tables/{name}/export` 别名
- Error Sanitization：所有 except 块 log 完整错误，返回 sanitized 消息
- Config 健壮性：VERSION 文件 fallback、temperature 验证、API key 警告
- Unique Count 优化：DuckDB COUNT(DISTINCT) 替代 pandas nunique

### 开发体验 (P4)
- per-table quality report：`qualityReports` Record 替代全局单例
- saved-queries MAX_QUERIES = 100
- sql-history-store partialize 只持久化 history 数组

### ESLint 修复
- `virtual-table/page.tsx`：`<a>` → `<Link>`
- `investigation-workspace.tsx`：`let` → `const`
- `global-search.tsx`：转义 `"` 实体

---

## v0.9.3 — Quality Optimization: Streaming, Code Quality, UX (2026-06-01)

### Streaming 架构优化
- SSE streaming 端点直连后端 `http://localhost:8000`，绕过 Next.js proxy 30s 超时
- 影响端点：`/ai/explain/stream`、`/ai/insights/stream`、`/ai/analyze-multi/stream`、`/ai/anomalies/stream`
- Streaming 中断时保留部分内容 + 重试按钮
- 自主分析进度条 + 步骤摘要 + 已用时间

### 代码质量提升
- ESLint 配置添加（no-unused-vars、no-console、no-explicit-any、exhaustive-deps）
- 清理未使用的导入和变量（6 个文件）
- API 函数 JSDoc 注释（12 个关键函数）

### 用户体验提升
- 首页查询历史列表（最近 5 条）
- 系统状态 loading skeleton

### Bug Fixes: Version Unification, Shortcuts, AI Timeouts
- 版本号统一（单一来源）
- 新建 `backend/VERSION` 文件作为 version single source of truth
- `backend/config.py` 改为从 VERSION 文件读取 `API_VERSION`
- 前端 sidebar、settings 页从 Zustand store 读取版本号（不再硬编码）
- Zustand store 默认版本改为 `"..."` loading 态

### 首页状态加载
- 新建 `use-system-status.ts` hook，AppShell mount 时自动获取 status + tables
- StatusPanel 移除重复轮询，改用 `useAIStatus()` hook
- 首页不再显示 "unknown" 状态

### 快捷键修复（12 个 bug）
- CommandPalette / GlobalSearch 添加 ESC 键关闭
- `?` 改为 `Ctrl+Shift+?`，不再在输入框中误触发
- 全局搜索从 `Ctrl+/` 改为 `Ctrl+Shift+F`，避免与 Monaco 注释切换冲突
- 清空编辑器从 `Ctrl+L` 改为 `Ctrl+Shift+K`
- 三个 AI 对话框（apply-template、report、save-template）添加 ESC + 背景点击关闭
- Save query 对话框添加背景点击关闭
- AiChart 全屏模式添加 ESC 退出
- `Ctrl+S` 改为 `Ctrl+Shift+S`，不拦截浏览器保存
- `Ctrl+A`/`Ctrl+D` 改为 `Ctrl+Shift+A`/`Ctrl+Shift+D`

### AI 分析性能和崩溃修复
- 后端 Anthropic client 设置 `timeout=30s`，stream `timeout=60s`
- `/ai/query`、`/ai/explain`、`/ai/insights` 添加 `asyncio.wait_for(60s)` 超时保护
- 前端 `apiFetch` 默认添加 `AbortSignal.timeout(60_000)`
- streamingContent 添加 100ms 节流，减少 O(n) 重渲染
- 自主分析移除冗余 `setSections` 调用
- FollowUpInput 添加 `mountedRef` 防止卸载后状态更新
- InvestigationWorkspace 改为选择性订阅 store 字段，修复 useCallback 无效问题

## v0.9.2 — Documentation Restructure (2026-06-01)

### 删除过时文件 (9 个)
- docs/reports/: KNOWN_RUNTIME_RISKS, MVP_READINESS_REPORT, CLEANUP_REPORT, TEST_COVERAGE_GAPS, PHASE4_READINESS
- docs/governance/: REPOSITORY_HEALTH
- docs/architecture/: SYSTEM_ARCHITECTURE_STATE, RERENDER_ANALYSIS
- 根目录: SESSION_SUMMARY_TEMPLATE

### 归档历史文件 (14 个 → docs/archive/)
- docs/architecture/: COMPONENT_SPLIT_PLAN, INVESTIGATION_WORKSPACE_PLAN, OWNERSHIP_FIX_PLAN, PHASE2_MIGRATION, STATE_REFACTOR_PLAN, STORE_OWNERSHIP_MAP, MIGRATION_NOTES
- docs/reports/: STABILIZATION_REPORT, FULL_RUNTIME_VALIDATION_REPORT, P4_RECHECK, P4_PRODUCTIZATION_ROADMAP, PRODUCTIZATION_GAP_REPORT
- docs/governance/: AB_TESTING
- docs/design/: PHASE3_CHANGELOG

### PROJECT_RULES.md 合并入 CLAUDE.md
- 测试报告要求 (2.4) → Testing Rules
- frontend_rules 引用 (2.7) → Documentation Rules
- 版本路线 (2.10) → 新增 Version Roadmap 章节
- Before Starting 移除 PROJECT_RULES.md 引用
- Scope Rules 补充 v0.7.x/v0.8.x/v0.9.x 完成范围

### 文档更新
- docs/README.md 全面重写（移除已删除/归档文件引用）
- docs/reports/NEXT_90_DAYS_PLAN.md 标记已完成任务
- docs/reports/PROJECT_MATURITY_REPORT.md 添加 v0.9.1 改善说明
- docs/architecture/UI_FLOW_MAP.md 移除 /workspace-legacy 引用
- docs/architecture/开发路线图.md 新增 v0.8.x/v0.9.x 条目
- docs/governance/FILE_SYSTEM_RULES.md 根文件列表更新

### 验证
- `npx next build` — PASS
- `npx tsc --noEmit` — PASS
- `python -c "from backend.main import app"` — PASS

## v0.9.1 — Security: API Key Purge & React Infinite Loop Fix (2026-06-01)

### Git 历史安全清理
- 使用 `git-filter-repo` 从所有历史 commit 中移除泄露的 API Key (`tp-c8bt3...`)
- 替换为 `REMOVED_API_KEY` 占位符
- 需要强制推送到远程仓库（历史已重写）

### React 无限渲染循环修复
- 修复 Zustand v5 selector 在 React 19 下返回新引用导致的无限重渲染
- `command-palette.tsx`: `runs` selector 改为 `useMemo` 派生
- `global-search.tsx`: `runs` selector 改为 `useMemo` 派生
- `page.tsx`: `recentRuns` selector 改为 `useMemo` 派生
- `tools-panel.tsx`: `runs` selector 改为 `useMemo` 派生
- `use-language.ts`: `toggleLanguage` 用 `useCallback` 包装，selector 逐个取值

### 验证
- `npx tsc --noEmit` — PASS
- `npx next build` — PASS
- `python -c "from backend.main import app"` — PASS
- Git 历史中 API Key 残留检查 — PASS（仅文档中的说明性引用）

## v0.9.0 — Legacy Route Removal, Docker Containerization, Demo Seed Data (2026-06-01)

### Legacy 路由移除 (Phase 2)
- 删除 `frontend-react/src/app/workspace-legacy/` 目录
- 移除 sidebar legacy 链接（替换为版本号 footer）
- 移除 settings page legacy 链接（替换为版本信息卡片）
- 清理 i18n key：`nav.legacy-workspace`（en.ts + zh.ts）
- `migrateFromLegacy()` 迁移桥保留（investigation-store + sql-editor-store）

### Docker 容器化
- `Dockerfile` — Python 3.11-slim 后端镜像，uvicorn 运行
- `Dockerfile.frontend` — Node 20-alpine 多阶段构建，standalone 输出
- `docker-compose.yml` — 双服务编排（backend:8000 + frontend:3000）
- `.dockerignore` — 排除 node_modules、.next、__pycache__、.git、.env、data/*.duckdb
- `next.config.ts` — 添加 `output: "standalone"` 支持 Docker standalone 构建

### Demo 种子数据
- `scripts/seed-demo-data.py` — 幂等种子脚本，从 testExcel/large_sales_data.csv 加载 demo_sales 表（50K 行）
- `backend/main.py` lifespan — 可选种子逻辑（`SEED_DEMO_DATA=true` 环境变量触发）
- `.env.example` — 新增 `# SEED_DEMO_DATA=true` 注释行

### 验证
- `npx next build` — PASS
- `npx tsc --noEmit` — PASS
- `python -c "from backend.main import app"` — PASS
- 种子脚本幂等性 — PASS

## v0.8.6 — Product Readiness Consolidation (2026-06-01)

### 文档整合 (Documentation Consolidation)
- 22 个文件重定位至合规目录（根目录违规 2 个、docs/ 顶层违规 20 个）
- 根目录违规文件 → `docs/reports/`（PHASE4_READINESS.md、P4_RECHECK.md）
- docs/ 报告文件 → `docs/reports/`（7 个）
- docs/ 架构文件 → `docs/architecture/`（7 个）
- docs/ v0.8 版本报告 → `docs/reports/v0.8/`（6 个）
- 治理文件命名修复：`claude-workflow.md` → `CLAUDE_WORKFLOW.md`
- 创建缺失目录：`skills/stable/`、`skills/archived/`、`error_logs/reports/`、`docs/reports/v0.8/`
- 重写 `docs/README.md` 索引，反映新目录结构

### 死代码清理 (Dead Code Removal)
- 删除 4 个孤立测试文件（引用已删除的 store 源文件）
  - `workflow-store.test.ts`、`ai-session-store.test.ts`
  - `query-tabs-store.test.ts`、`sql-workspace-store.test.ts`

### 配置修复 (Configuration Fixes)
- `.env.example`: 真实 API Key 替换为占位符（安全修复）
- `frontend-react/package.json`: 版本从 `0.3.10` 修正为 `0.8.6`

### 治理
- `docs/README.md`: 全面重写文档索引
- `README.md`: 新增 Prerequisites、.env 配置步骤、版本路线图更新
- `CURRENT_SESSION.md`: 更新至 v0.8.6

## v0.7.5 — Stability & System Validation (2026-05-25)

### 系统健康诊断 (后端)
- 新增 `GET /api/health/system` 综合诊断端点
- `data_service.py`: 新增 `get_system_health()` 汇聚多维度状态
  - DB 连接状态、AI 配置、调度器任务/worker、查询历史统计、临时文件
- `config.py`: `API_VERSION` 从 `"0.5.0"` 更新至 `"0.7.5"`

### Scheduler 生命周期 (后端)
- `main.py` lifespan: 新增 `start_worker()` / `stop_worker()` 调用
- 之前 scheduler worker 从未被启动，现在随应用自动启停

### 前端状态韧性 (前端)
- 7 个持久化 store 新增 `merge` 函数，防止 localStorage 损坏导致数据丢失
  - `query-tabs-store`: 验证 tabs 数组结构
  - `saved-queries-store`: 验证 queries 数组
  - `sql-history-store`: 验证 history 数组
  - `analysis-store`: 验证 runs 数组
  - `ai-session-store`: 验证 turns 数组
  - `template-store`: 验证 templates 数组
  - `schedule-store`: 验证 tasks 数组
- `analysis-store`: `onRehydrateStorage` 自动调用 `recoverInterruptedRuns()`
- `analysis-store`: `importRuns` 强制 `MAX_HISTORY` 上限（保留 saved runs）
- `schedule-store`: 移除重复 `generateId`，统一使用 `@/utils/id`

### E2E 测试
- `e2e/helpers.ts`: 共享测试工具函数
- `e2e/user-journey.spec.ts`: 7 个测试覆盖完整用户旅程
  - 上传 → 查询 → 聚合 → 分页 → 错误处理 → AI 面板 → 重载持久化
- `e2e/persistence.spec.ts`: 5 个测试覆盖持久化与重载流程
  - Tab SQL 持久化、新 Tab 持久化、表重载、重载后查询、快速重载稳定性
- `e2e/global-setup.ts`: 后端健康检查前置

### 治理
- `开发路线图.md`: 新增 v0.7.4/v0.7.5 条目，v0.7.3 标记为 Done
- `README.md`: 版本路线图更新、AI Intelligence Layer 功能列表、新 API 端点
- `CURRENT_SESSION.md`: 更新至 v0.7.5

## v0.7.4 — Runtime Stability & Resource Lifecycle (2026-05-25)

### Import-Time Side Effect Elimination (后端)
- `query_history.py`: `QueryHistory.__init__()` 不再调用 `_init_table()` / `_load_from_db()`，改为 lazy init（首次 `add()` / `get_all()` 时才连接 DuckDB）
- `data_service.py`: `_db` / `_executor` 从模块级直接实例化改为 `get_db()` / `get_executor()` lazy getter
- 消除 `uvicorn --reload` 下的 DuckDB 文件锁定根因：导入时不再触发 `duckdb.connect()`

### 引用链更新 (后端)
- `routes/query.py`: `_executor` → `get_executor()`
- `routes/tables.py`: `_db` → `get_db()`
- `routes/analyze.py`: `data_service._db` → `data_service.get_db()`
- `services/ai_pipeline.py`: `_executor` → `get_executor()`
- `tests/ai/conftest.py`: `_executor` → `get_executor()`
- `tests/test_upload_quality_routes.py`: 适配新的 lazy getter

### FastAPI Lifespan (后端)
- `main.py`: 新增 `lifespan` 上下文管理器，集中管理启动/关闭
- 启动时预热 DB 连接（减少首次请求延迟）
- 关闭时调用 `get_db().close()` 确保 DuckDB 连接干净释放

### DuckDB 连接加固 (后端)
- `db_manager.py`: `close()` 方法增加 `try/except` 防护

### 测试
- 后端 338 pass / 4 pre-existing fail（不变）
- 前端 160/160 pass
- TypeScript: clean
- Frontend build: OK

## v0.7.3 — System Cleanup & Pipeline Refactor (2026-05-25)

### Pipeline Refactor (后端)
- `ai_pipeline.py` 提取 8 个共享辅助函数：`_make_step_result`, `_check_step_guardrails`, `_check_step_budget`, `_build_dependency_context`, `_generate_step_sql_with_retry`, `_build_diagnostics`, `_build_executive_summary`, `_determine_overall_status`
- 新增 `_execute_plan_steps()` 统一步骤执行逻辑，streaming 和 non-streaming 共用
- non-streaming `run_autonomous_analysis()` 补齐 step-level retry（之前缺失，streaming 版有）
- `_infer_column_types()` 内部变量名 bug 修复（`c` → `col`）

### Shared Utility Module (后端)
- 新建 `backend/services/shared_utils.py` — 共享 `_truncate()` 工具函数
- `diff_engine.py` / `report_builder.py` 改为从 shared_utils 导入，消除重复代码

### Dead Code & Import Cleanup (后端)
- `trace.py`: 移除未使用的 `save_to_file()` 方法及 `import os`
- `routes/ai.py`: 移除冗余 `import json as _json`，将 `import re` 提升为顶层导入
- `ai_analyst.py`: 将局部 `import re` 提升为顶层导入

### Stale Docs & File System Cleanup (文档)
- `版本记录.md`: 移除重复的 v0.3.8 条目
- `开发路线图.md`: 更新至 v0.7.x（之前停留在 v0.5.6）
- `PROJECT_RULES.md`: 版本表更新（v0.6.x Done, v0.7.x Current）
- `FILE_SYSTEM_RULES.md`: 移除已归档的 `docs/frontend_rules/` 引用
- 归档 `docs/performance/performance-baseline.md` → `docs/archive/`
- 移除孤立空目录 `frontend/`

### 测试
- 移除 `test_trace.py::test_save_to_file`（方法已删除）
- 98 个相关模块测试全部通过

## v0.7.2 — AI Session Quality Layer (2026-05-25)

### Anomaly Precision Scoring & Adaptive Filtering (后端)
- `_detect_numeric_columns()` 修复：多行扫描（前 20 行），要求 ≥80% 数值，修复首行 null 导致误判
- `detect_anomalies()` 新增 `min_deviation_score` 参数（默认 1.5），过滤低偏离异常
- 新增 `adaptive` 参数（默认 True），P25 自适应阈值过滤底部 25% 边际异常（列内 ≥4 个异常时生效）
- 新增 `precision_score` 字段：`min(1.0, deviation_score / max_threshold)`，标识统计置信度
- 异常上限 50 个，按 deviation_score 降序排列
- `AnomalyDetectRequest` 新增 `min_deviation_score` + `adaptive` 字段
- anomaly_interpretation prompt 增加 precision_score 引导

### Insight Evidence Scoring & Filtering (后端 + 前端)
- `_score_and_filter_insights()` 确定性评分：`confidence*0.5 + severity_w*0.3 + impact_w*0.2`
- 过滤 `confidence < 0.3` 的洞察（记录 filtered_insights_count）
- 按 evidence_score 降序排列，每个洞察附带 `evidence_score` 字段
- 趋势过滤 `confidence < 0.2`
- 前端：低置信度标签（`< 0.4` 显示 "Low confidence"）
- `generate_insights()` 集成评分和过滤

### Self-Evaluation Wiring (后端 + 前端)
- `evaluate_analysis()` 服务函数：从 routes/ai.py 提取逻辑，支持 JSON 解析重试
- routes `/ai/evaluate` 重构为委托到服务函数
- 前端：分析完成后自动调用 aiEvaluate()（非阻塞，try/catch）
- 新增 `EvaluationResult` 前端状态 + 质量评估渲染面板
- 置信度条（绿/琥珀/红）+ 维度标签（完整性/准确性/可操作性）
- `AnalysisRun` 的 `updateRun` 类型新增 `evaluation` 字段

### Quality Gates (后端 + 前端)
- `_apply_quality_gates()` 确定性规则：confidence < 0.4、维度 low、异常率 > 50%、无发现
- 返回 `{passed, warnings[:3], checks_run}`
- 集成到 `evaluate_analysis()` 响应
- 前端：质量警告显示在评估面板中

### Token Budget & Robustness
- `OPERATION_BUDGETS` 新增 `self_evaluation`（3000/1024）
- `self_evaluation.py` 空 sections 保护 + trace 包含

### 测试
- `test_anomaly_detector.py` 新增 7 个测试（多行扫描、min_deviation、adaptive、precision_score、cap、排序）
- 31 anomaly detector tests, 29 self-eval + token budget tests, 160 frontend tests 全部通过

## v0.7.1 — Multi-Turn Analysis Continuity (2026-05-25)

### 增强上下文模型（后端）
- `FollowUpContext` 新增 `prior_key_findings: list[str]` 和 `investigation_summary: str`
- `build_follow_up_context()` 增强：prior_key_findings 渲染为编号列表（cap 5），investigation_summary 显示时截断 previous_insight_summary 到 200 字符
- `InsightsRequest` 新增 `prior_context: str`，`AnalysisPlanRequest` 新增 `prior_findings: list[str]`
- `MultiAnalyzeRequest` 新增 `prior_findings: list[str]`
- insights prompt 新增 `prior_context` optional var + system prompt 引导
- analysis_plan prompt 新增 `prior_findings` optional var + system prompt 引导
- `generate_insights()`/`generate_insights_stream()` 新增 prior_context 参数
- `generate_analysis_plan()` 新增 prior_findings 参数
- `run_autonomous_analysis()`/stream 新增 prior_findings 透传到 plan 生成
- insights token budget max_history_turns 0→4

### 结构化压缩（前端 session store）
- `compressTurns()` 升级：用户问题 120 字符（原 80）、assistant 保留完整 SQL（原 100 字符截断）、提取首句作为 Finding
- 新增 `keyFindings: string[]` — 累积关键发现（cap 10，去重）
- 新增 `investigationSummary: string | null` — 线程级摘要
- 新增 `addKeyFinding()` — 去重 + cap
- 新增 `getContextForInsights()` — 构建 prior_context 文本
- 新增 `getContextForPlan()` — 返回 keyFindings.slice(0,5)
- 持久化 keyFindings + investigationSummary

### Drill-Down 流
- `AnalysisRun` 新增 `drillDownFrom?: { runId, findingIndex, findingText }`
- `drillDownRun()` 方法：从高严重性洞察创建 linked autonomous run
- 前端 insights 渲染后展示 drill-down 按钮（高严重性发现）
- 自动积累 key findings：explain 完成后提取首句、insights 完成后提取高严重性洞察、anomalies 完成后提取摘要

### 前端 API 集成
- `FollowUpContext` 新增 prior_key_findings + investigation_summary
- `aiInsights()` 新增 priorContext 参数
- `streamAiInsights()` 新增 priorContext 参数
- `streamAiAnalyzeMulti()` 新增 priorFindings 参数
- follow-up-input 富化 FollowUpContext（自动附加 keyFindings + investigationSummary）
- analysis panel insights 模式自动传递 priorContext
- analysis panel autonomous 模式自动传递 priorFindings

### i18n
- 新增 4 个键：ai.drill-down, ai.drill-down-hint, ai.prior-context, ai.key-findings

### 测试
- `tests/test_follow_up_context.py` — 12 个测试（向后兼容、findings 渲染、截断逻辑、token 预算范围）
- `ai-session-store.test.ts` — 更新 + 新增 keyFindings 去重/cap、getContextForInsights/Plan、结构化压缩测试
- `analysis-store.test.ts` — 新增 drillDownRun 创建/字段/evolution chain 测试

### 验证
- 前端构建: PASS
- 后端导入: PASS
- 前端测试: 160/160 PASS
- 后端测试: 149/149 PASS (core) + 26 PASS (AI endpoints/pipeline)
- TypeScript: PASS

## v0.7.0 — Anomaly Detection Engine (2026-05-25)

### 异常检测引擎
- 新建 `backend/services/anomaly_detector.py` — 纯 Python 统计异常检测
  - Z-score 检测（正态分布，阈值可配，默认 3.0）
  - IQR 检测（偏斜分布，倍数可配，默认 1.5）
  - Auto 模式（自动选择：skewness>2 回退 IQR）
  - 支持指定列或自动检测全部数值列
  - 返回 anomalies[], summary{}, column_stats{}
- 新建 `backend/prompts/anomaly_interpretation.py` — LLM 解读异常业务意义
  - 评估 anomaly_type (outlier_spike/drop/distribution_shift/unexpected_value)
  - 输出 severity, business_meaning, suggested_investigation, confidence
- 注册到 registry.py（10→11 prompts）
- OPERATION_BUDGETS 新增 anomaly_interpretation (4000/1024)

### 异常检测 API
- 新增 `POST /api/ai/anomalies` — 检测+解读异常
- 新增 `POST /api/ai/anomalies/stream` — SSE 流式：先返回统计结果，再流式解读
- `detect_and_interpret_anomalies()` — 统计检测→LLM 解读→合并结果
- `detect_and_interpret_anomalies_stream()` — 流式变体

### 前端异常展示
- 新增 `AnomalyItem`, `AnomalyInterpretation`, `AnomalyResult` 类型
- 新增 `aiDetectAnomalies()` 和 `streamAiDetectAnomalies()` API 客户端
- AnalysisMode 新增 "anomalies"，AnalysisRun 新增 anomalies 字段
- AI Analysis Panel 新增异常检测模式：摘要/详情表格/解读/建议操作
- SQL Workspace 新增异常检测按钮（amber 色区分）
- 新增 ~15 个 i18n 键（中英文）

### 测试
- `tests/test_anomaly_detector.py` — 24 个测试
  - 核心检测：空数据、无数值列、不足数据、z-score/IQR/auto 检测
  - 边界：全 null 列、混合 null、干净数据误报率、指定列
  - 辅助函数：数值列检测、数值提取、统计量、百分位、偏度

### 验证
- 前端构建: PASS
- 后端导入: PASS
- 前端测试: 142/142 PASS
- 后端测试: 71/71 PASS (含新旧测试)
- TypeScript: PASS
- Prompts: 11 registered

## v0.6.4 — Reliability & Scale (2026-05-25)

### 服务端分页（ISSUE-006）
- 新增 `execute_paginated(sql, offset, limit, timeout_ms)` — SQL 级 OFFSET/LIMIT 分页
- 修改 `QueryRequest` 添加 `offset` 和 `timeout_ms` 参数
- 前端 `sql-workspace-store.ts` 添加分页状态（offset, totalRows, hasMore, isLoadingMore）
- 新增 `loadMore()` 方法实现无限滚动加载
- DataTable 连接 `onLoadMore` + `hasMore` + `isLoading` props

### 查询历史持久化（ISSUE-002）
- `query_history.py` 添加 DuckDB 持久化（`query_history` 表）
- 启动时从 DuckDB 加载历史（最多 200 条）
- 写入时同步到 DuckDB，自动清理旧条目
- 前端 `sql-history-store.ts` 添加 `fetchHistory()` 从后端读取历史
- 组件挂载时自动调用 `fetchHistory()`

### 大数据集稳定性
- `db_manager.py` 添加 `timeout_ms` 参数，设置 `statement_timeout`
- `query_executor.py` 超时时返回友好错误信息（提示添加 LIMIT）
- COUNT 查询使用较短超时（60s）

### Query Memory/Cache Protection
- `sql-workspace-store.ts` 添加 queryResult 大小守卫（50MB 警告）
- `sql-history-store.ts` 添加 localStorage 大小守卫（2MB 限制）
  - 截断长 SQL（>1000 字符）
  - 截断长 error（>500 字符）
  - 超限时自动丢弃最旧条目
- `analysis-store.ts` 优化压缩逻辑
  - 保存和未保存 runs 都压缩
  - Chart data 限制 100 点
  - Step data 限制 50 行
  - Trace events 限制 20 条
  - 4MB 超限时优先丢弃最旧 runs

### Long-Session Reliability
- `api.ts` SSE 流添加自动重连机制
  - 最多 2 次重试
  - 指数退避（1s, 2s）
  - 仅在未收到数据时重试
- `consumeSseStream` 和 `consumeSseStreamGeneric` 都支持重连

### 测试
- 修复 `api.test.ts` 中的测试（适配新的 offset 参数）
- 前端测试: 142/142 PASS

### 验证
- 前端构建: PASS
- 后端导入: PASS
- TypeScript: PASS
- 前端测试: 142/142 PASS

### 风险
- DuckDB `statement_timeout` 可能在某些版本不支持（已添加 try-catch）
- SSE 重连可能导致重复数据（依赖 onEvent 去重）

### Rollback
- 后端: 移除 `execute_paginated`，恢复 `execute` 调用
- 前端: 移除 `loadMore` 和 `fetchHistory`，恢复原始 `executeQuery` 签名

## v0.6.3 — Analysis Operations Layer (2026-05-25)

### Analysis Comparison/Diff
- 新增 `backend/services/diff_engine.py` — 纯 Python 结构化 diff 引擎
- `diff_runs()` 比较 sections (added/removed/changed/unchanged)、SQL 变化、metrics (tokens/llm_calls/row_count)
- 新增 `POST /api/ai/compare` 端点
- 前端 `analysis-store.ts` 新增 `compareRuns(idA, idB) => ComparisonResult` (纯前端计算)
- 新增 `diff-panel.tsx` — 双列对比视图，高亮差异，metrics 卡片

### Shareable Analysis Bundle
- 新增 `POST /api/ai/bundle/export` — 打包 runs 为 bundle JSON
- 新增 `POST /api/ai/bundle/import` — 验证并导入 bundle
- `analysis-store.ts` 新增 `importRuns()` — 批量导入，自动分配新 id

### Timeline/History Evolution
- `analysis-store.ts` 新增 `getEvolutionChain(runId)` — 沿 parentRunId 链追溯完整版本历史
- 新增 `timeline-evolution.tsx` — 垂直时间线可视化，展示版本演进

### AI Self-Evaluation + Confidence Diagnostics
- 新增 `backend/prompts/self_evaluation.py` — AI 自评 prompt (completeness/accuracy/actionability/confidence)
- Prompt 注册到 `registry.py`
- 新增 `POST /api/ai/evaluate` 端点
- `AnalysisRun` 新增 `evaluation?: EvaluationResult` 字段
- 前端展示 confidence badge (绿/黄/红) + diagnostics + suggested improvements

### Scheduled Analysis Workflow
- 新增 `backend/services/scheduler.py` — `SchedulerManager` 单例，持久化到 `data/scheduled_tasks.json`
- 新增 `backend/runtime/scheduler_worker.py` — daemon 线程，每 60s 检查并执行到期任务
- 新增 5 个 API 端点: POST/GET/DELETE/PATCH `/ai/schedule`, GET `/ai/schedule/{id}/results`
- 新增 `schedule-store.ts` — 前端 Zustand persist 存储调度配置
- 新增 `schedule-dialog.tsx` — 调度任务管理 UI

### 国际化 (i18n)
- 新增 ~20 个 i18n 键 (en.ts + zh.ts)：diff、timeline、evaluation、schedule

### 测试
- `tests/test_diff_engine.py` — 16 个测试
- `tests/test_scheduler.py` — 12 个测试
- `tests/test_self_evaluation.py` — 8 个测试

### 验证
- 前端构建: PASS
- 前端测试: 142/142 PASS
- 后端测试: 302 PASS (16 FAILED — pre-existing AI evaluation tests need API key)
- TypeScript: PASS
- 后端导入: PASS

## v0.6.2 — Analyst Workflow Layer (2026-05-25)

### 分析模板 (Analysis Templates)
- 新增 `template-store.ts` — Zustand persist 存储，最多 20 个模板
- `AnalysisTemplate` 类型：name, description, steps, sourceRunId, sourceTable
- 新增 `save-template-dialog.tsx` — 从分析运行创建模板
- 新增 `apply-template-dialog.tsx` — 选择模板 → 选择目标数据集 → AI 适配问题 → 执行
- 新增后端 prompt `template_adaptation.py` — 将模板问题适配到新数据集 schema
- 新增 `POST /ai/adapt-template` 端点 — LLM 驱动的问题适配
- 新增 `aiAdaptTemplate()` 前端 API 函数

### 报告生成 (Report Generation)
- 新增 `backend/services/report_builder.py` — 纯 Python Markdown 报告构建器
- 支持：目录、多运行摘要、步骤结果、执行摘要、可选追踪附录、可选数据样本
- 新增 `POST /ai/generate-report` 端点 — 接收前端运行数据，返回 Markdown
- 新增 `report-dialog.tsx` — 多选运行、标题输入、选项开关、预览（react-markdown）、下载 .md
- 新增 `generateReport()` 前端 API 函数

### 数据集生命周期 (Dataset Lifecycle)
- 新增 `DatasetMeta` 类型：table, uploadTime, analysisCount, lastAnalyzedAt, qualityScore
- `data-store.ts` 新增 `getDatasetMeta()` — 跨 store 读取分析历史
- `data_service.py` 新增 `_UPLOAD_TIMESTAMPS` — 上传时记录 UTC 时间戳
- `list_tables()` 响应新增 `uploadTime` 字段
- `table-management-panel.tsx` 每表显示分析次数标记

### 工作流持久化增强 (Workflow Persistence)
- `analysis-store.ts` 新增 `notes?: string` 字段 — 用户注释
- 新增 `duplicateRun()` — 克隆运行（新 id/时间戳，parentRunId 链接）
- 新增 `updateRunNotes()` — 更新注释（防抖保存 500ms）
- 新增 `recoverInterruptedRuns()` — 将 status=running 的运行标记为 error
- `analysis-workspace-panel.tsx`：新增备注文本域、复制按钮、保存为模板按钮

### 国际化 (i18n)
- 新增 ~26 个 i18n 键（en.ts + zh.ts）：分析备注、数据集生命周期、模板、报告生成

### 测试
- `tests/test_report_builder.py` — 16 个测试（build_report 10 个 + helpers 6 个）
- `frontend-react/src/stores/__tests__/analysis-store.test.ts` — 17 个测试
- `frontend-react/src/stores/__tests__/template-store.test.ts` — 8 个测试

### 验证
- 前端构建: PASS
- 前端测试: 142/142 PASS
- 后端测试: 264 PASS (含新增 report_builder 测试，DuckDB 锁失败为预存问题)
- TypeScript: PASS
- 后端导入: PASS

## v0.6.1 — Analysis Workflow Persistence (2026-05-25)

### Saved Analysis Runs
- `AnalysisRun` 新增 `saved: boolean`、`version: number`、`parentRunId?: string` 字段
- 新增 `saveRun`/`unsaveRun`/`deleteRun`/`rerunRun`/`exportRun` 操作
- FIFO 保护：`addRun` 裁剪时不会删除已保存的运行记录
- `partialize` 按条件持久化：saved 运行完整保存，unsaved 运行截断（500字符）
- 大小守卫：localStorage 超 4MB 时自动裁剪最老的 unsaved 运行
- UI：分析历史面板新增星标/取消星标、删除、导出 JSON、重新运行按钮

### AI 会话持久化 & 记忆压缩
- `ai-session-store` 添加 Zustand `persist` 中间件（key: `"ai-session"`）
- 新增 `compressedSummary` 字段和 `compressTurns()` 压缩逻辑
- 超过 15 轮时自动压缩：保留最近 8 轮，旧轮次压缩为摘要
- 新增 `getContextForApi()` 方法：返回压缩摘要 + 最近轮次 + 上下文元数据
- `partialize`：持久化 8 轮 + 摘要 + 上下文字段

### 重试/恢复 (Retry/Recovery)
- `_call_llm`/`_call_llm_stream` 新增瞬态错误重试（最多 2 次，指数退避 1s/3s）
- 仅重试：RateLimitError、APITimeoutError、APIConnectionError、InternalServerError
- 不重试：AuthenticationError、BadRequestError 等客户端错误
- 流式调用：仅在流建立前重试，已 yield 内容后不重试
- Pipeline 步骤级重试：SQL 生成失败时注入错误上下文重试一次
- 新增 `step_retry` SSE 事件：前端可显示重试状态

### 版本追踪 & 治理
- `TraceRecorder.to_dict()` 新增 `schema_version: "1.0"`
- `rerunRun` 自动设置 `parentRunId` 和 `version + 1`
- `exportRun` 导出完整运行快照（JSON）
- 新增测试：`tests/test_ai_analyst_retry.py`（9 个测试）

### 验证
- 前端构建: PASS
- 前端测试: 117/117 PASS
- 后端测试: 248/248 PASS
- TypeScript: PASS
- 后端导入: PASS

## v0.6.0 — Meta Governance & Autonomous QA (2026-05-25)

### 语言治理 (Language Governance)
- 默认语言从 `en` 改为 `zh`：i18n、workspace-store、所有后端 API 端点、trace
- `locale.py` 新增 `zh-CN`/`zh-TW` 变体映射，统一归一化为 `zh`
- CLAUDE.md / PROJECT_RULES.md 新增语言治理规则：人类沟通中文、代码英文、AI 输出跟随 UI 语言

### 文档清理 (Documentation Cleanup)
- 归档错误项目的 Vue/Element Plus 参考文件 (`docs/frontend_rules/` → `docs/archive/frontend_rules/`)
- 归档已完成的版本计划 (`v0.5.1-plan.md`, `v0.5.4-plan.md` → `docs/archive/`)
- 修复 `docs/README.md`：版本号、REPOSITORY_HEALTH.md 链接、移除已归档引用
- 重新生成 `REPOSITORY_HEALTH.md` (v0.5.9，评分 91/100)
- 更新 `TESTING_STRATEGY.md`：修正测试数量 (117 FE, 161 BE)，新增 AI 测试章节
- 更新 `ERROR_ANALYSIS.md`：新增 AI Pipeline 错误类别（LLM 超时、SSE 中断、token 溢出、guardrail 违规等）
- 更新 `AB_TESTING.md`：关闭已决定的 A/B 测试（Zustand, Tailwind）
- 标记 `performance-baseline.md` 为 DRAFT

### 后端单元测试 (Backend Unit Tests)
- `tests/test_guardrails.py` — AnalysisGuard 全覆盖（defaults, check_before_step, record_step_result, check_after_all, to_dict, _chain_depth）
- `tests/test_trace.py` — TraceRecorder 全覆盖（init, record_llm_call, violations, set_plan, finish, to_dict, save_to_file）
- `tests/test_token_budget.py` — Token Budget 全覆盖（estimate_tokens, truncate_text, truncate_rows, get_budget, WorkflowTokenTracker）
- `tests/test_ai_endpoints.py` — AI API 端点 mock 测试（status, query, explain, insights, chart-suggest, streaming, semantics）
- `tests/test_ai_pipeline_unit.py` — Pipeline 辅助函数测试（_infer_column_types, _derive_step_summary）
- 测试数量：239 BE (161+78 new) + 117 FE

### AI 可靠性修复 (AI Reliability Fixes)
- SSE 超时机制：`consumeSseStream` (60s) 和 `consumeSseStreamGeneric` (120s) 添加无活动超时，自动 abort 并报错
- 类型推断扫描窗口：10 → 50 行，改善全 null 列的检测
- JSON 序列化硬化：`_safe_serialize()` 处理 datetime→ISO, Decimal→float, bytes→base64
- 死胡同恢复：autonomous analysis 所有步骤失败时产出 diagnostic 事件（含失败摘要）

### E2E 测试 (E2E Tests)
- `e2e/ai-workflow.spec.ts` — AI 用户旅程（上传→查询→AI 分析面板→刷新持久化）
- `e2e/ai-error-handling.spec.ts` — 错误处理（空 SQL、无效 SQL、快速重执行、hydration 错误、主题切换）
- `e2e/fixtures/sample-data.csv` — E2E 测试数据
- `scripts/run-all-tests.sh` — 全套测试运行脚本

### 验证
- 前端构建: PASS
- 前端测试: 117/117 PASS
- 后端测试: 239/239 PASS
- TypeScript: PASS
- 后端导入: PASS

## v0.5.9 — Core AI Functionality Stabilization (2026-05-25)

### Explain Pipeline Repair
- `_call_llm` now concatenates ALL text blocks from Claude API (was: only first block, rest dropped)
- Explanation prompt hardened: instructs LLM to output clean markdown, no literal `\n` sequences, no code fences

### Follow-up Redesign
- Follow-up input now auto-executes SQL (`execute: true` was `false`) — returns results + explanation directly
- `followUpQuestion` state properly reset between analysis runs (was: leaked)

### AI Output Sanitization
- Backend: text block concatenation fix prevents truncated/empty explanations
- Frontend: `AnalysisResult` type now includes `insights`, `trends`, `data_quality_notes`, `suggested_next_steps` — removed unsafe `as unknown as` casts

### Trace Accuracy
- `trace.finish()` now receives actual status: `success` / `partial` / `error` (was: always `success`)
- Response `status` field reflects real outcome based on step failures and guardrail violations

### Autonomous Analysis Stability
- `_infer_column_types` scans all 10 rows with hierarchical type widening (was: broke after first non-null)
- `max_step_time_seconds` guardrail now enforced — checks per-step elapsed time at each step start
- `import time` moved to top of file (was: after function definition)

### Streaming State Fixes
- Running-step indicator no longer flickers — cleanup moved from `onStepResult` to `onSummary`/`onDone`
- SSE parsing deduplicated: new `consumeSseStreamGeneric()` used by `streamAiAnalyzeMulti` (was: 80 lines duplicated)

### Store Persistence Fix
- `analysis-store` partialize now preserves section titles + truncated content, chart metadata, step metadata, trace summary (was: zeroed everything — history was empty after refresh)

### Theme Detection Fix
- `useCodeTheme()` now uses `useThemeStore` instead of unreliable CSS variable heuristic

### Validation
- Frontend build: PASS
- Frontend tests: 117/117 PASS
- Backend tests: 161/161 PASS
- Backend import: PASS
- TypeScript: PASS

## v0.5.8 — Product Reliability & User Journey Validation (2026-05-25)

### Theme SSR/Hydration Fix
- `applyTheme()` now guards against SSR (`typeof document === "undefined"`)
- Root layout uses inline script to read theme from localStorage before paint — no flash
- Body uses CSS variables (`var(--bg-primary)`, `var(--text-secondary)`) instead of hardcoded Tailwind colors
- `suppressHydrationWarning` on `<html>` to prevent false positives

### Chart Rendering Fix
- "charts" mode now renders actual `<AiChart>` components (not just markdown)
- "full-analysis" mode now renders charts from `chart_suggestions` when data available
- Backend `/analyze/{table}` response now includes `data` field (sample rows)
- `AnalysisResult` TypeScript interface updated with `data` field

### SSE Streaming Hardening
- `consumeSseStream()` drains remaining buffer after stream loop ends — no more lost final events
- `streamAiAnalyzeMulti()` same buffer drain fix
- All `StreamingResponse` endpoints now include `Cache-Control: no-cache`, `Connection: keep-alive`, `X-Accel-Buffering: no` headers

### Backend Bug Fixes
- `_infer_column_types()` scans up to 10 rows per column (was: first row only) — robust NULL handling
- `generate_insights()` error handler uses `locals()` instead of `dir()` for variable check
- `TraceRecorder` uses shared `estimate_tokens()` from `token_budget` (was: duplicated `// 3` heuristic)

### Follow-up UX Fix
- `SuggestedQuestions` click handler now actually populates follow-up input (was: no-op)
- `FollowUpInput` supports controlled `question`/`onQuestionChange` props
- New `followUpQuestion` state bridges suggested questions → follow-up input

### Store Safety
- `analysis-store` uses `partialize` to exclude large fields (sections, multiResult, chartSpecs, trace) from localStorage — prevents 5MB quota overflow
- Run metadata (id, mode, question, timestamp, status) still persisted for history

### Governance
- Moved `REPOSITORY_HEALTH.md` from root to `docs/governance/` (file system rules violation)

### Validation
- Frontend build: PASS
- Frontend tests: 117/117 PASS
- Backend tests: 161/161 PASS
- Backend import: PASS

## v0.5.7 — Stability & AI Correctness Sprint (2026-05-24)

### SSR Hydration Fix
- Fixed SSR hydration safety in 6 persisted Zustand stores

### AI Analysis Chain Fix
- Fixed full-analysis mode to produce structured insights
- Fixed follow-up context corruption

### Streaming Pipeline Hardening
- Fixed stale closures, SSE AbortController, JSON.parse safety

## v0.5.6 — Analysis Workspace (2026-05-24)

### Analysis Store
- New `analysis-store.ts` — persisted Zustand store for analysis run history + trace data
- `AnalysisRun` type: id, mode, question/table, timestamp, status, sections, chartSpecs, multiResult, trace
- `TraceSnapshot` type: mirrors backend TraceRecorder output (events, token totals, guardrail violations)
- MAX_HISTORY=20 cap to prevent store bloat
- Actions: addRun, updateRun, setActiveRun, getActiveRun, clearHistory

### AIAnalysisPanel Refactor
- Extracted 5 sub-components from the 850-line monolith:
  - `analysis-section.tsx` — markdown section renderer (copy, syntax highlight, tables)
  - `step-results.tsx` — multi-step result cards
  - `trace-timeline.tsx` — trace event visualization (latency bars, token counts)
  - `analysis-header.tsx` — header bar with mode label and action buttons
  - `follow-up-input.tsx` — follow-up question input
- `ai-analysis-panel.tsx` slimmed to orchestration layer (~350 lines)

### Analysis Workspace Panel
- New `analysis-workspace-panel.tsx` — dedicated right panel tab
- Three sections: Session History, Active Run Detail, Trace Inspector
- Wired into page.tsx as third tab alongside Preview and History
- Trace data extracted from streamAiAnalyzeMulti onDone event

### API Changes
- `MultiStreamEvent` extended with trace, token_budget, guardrail_violations fields
- `MultiStreamCallbacks.onDone` now receives full event data (not just elapsedMs)

### Skills
- Created `skills/SKILL_REGISTRY.md` (was missing since v0.5.5)
- Added `skills/active/analysis-workspace.md`

### Validation
- TypeScript: Pass
- No backend changes

## v0.5.5 — Documentation Governance (2026-05-24)

### Documentation Cleanup
- Archived 4 stale reports (v0.3.12-bug-hunt, v0.4.0-diagnostics, ISSUE-006-decision, coverage-improvement-plan) to `docs/archive/reports/`
- Deleted duplicate `docs/skills/SKILL_REGISTRY.md` (active registry is `skills/SKILL_REGISTRY.md`)
- Moved `docs/v0.5.1-plan.md` to `docs/architecture/`
- Updated `PROJECT_RULES.md` version roadmap (v0.5.x = AI System Engineering)
- Updated `docs/architecture/开发路线图.md` (v0.3.x–v0.5.x actual history)
- Updated `docs/architecture/项目架构说明.md` (AI layer, v0.5.4 architecture)
- Updated `README.md` (AI features, /ai/* endpoints, current tech stack)
- Updated `CLAUDE.md` and `CURRENT_SESSION.md` (version alignment)
- Updated `docs/README.md` (v0.5.5 doc index)
- Updated `docs/governance/` lifecycle docs (skills path, doc state table)

### Governance Rules
- Established documentation lifecycle: active → stable → archived
- Established skills lifecycle: `skills/active/` → `skills/stable/` → `skills/archived/`
- All docs must reference current version, not "Planned"
- Architecture docs must include AI layer as first-class component

### Validation
- No business logic changes
- No code changes
- All doc references verified against actual codebase

## v0.5.4 — AI System Engineering: Prompt Architecture, Token Budget, Guardrails, Trace, Evaluation (2026-05-24)

### Module 1: Prompt Architecture Layer (已完成)
- 创建 `backend/prompts/` 目录，8 个 prompt 模块
- 定义 `PromptContract` 数据类：name, purpose, required/optional vars, response_format, max_output_tokens
- 每个模块导出：CONTRACT, SYSTEM_PROMPT, build_user_message()
- `registry.py` 集中注册所有 prompt 合约
- `locale.py` 统一语言处理（从 ai_analyst.py 迁移）
- `ai_analyst.py` 所有 prompt 常量和 user message 构建逻辑迁移至 prompts 层
- `ai_pipeline.py` 移除 `_SUMMARY_SYSTEM`，改为导入 `summarizer` 模块
- 服务层零 prompt 文本，全部从 prompts 导入
- 新增 skill: `prompt-architecture.md`

### Module 2: Token Budget System (已完成)
- 创建 `backend/runtime/token_budget.py`
- `TokenBudget` 数据类定义 per-operation 预算（input/output/schema/sample/history/workflow_total）
- `estimate_tokens()` 启发式估算（len//3），禁止第三方 tokenizer
- `truncate_text()` / `truncate_rows()` 截断工具
- `WorkflowTokenTracker` 多步骤 workflow token 追踪，25K 总预算
- 8 类 operation 预算配置：sql_gen(3K), explain(6K), insights(5K), charts(3K), semantics(3K), questions(2K), plan(3K), summarizer(3K)
- `_call_llm` / `_call_llm_stream` 集成 budget enforcement + tracker recording
- 所有 public 函数使用 budget 配置替代硬编码值（行数、max_tokens）
- autonomous analysis 集成 tracker: 超预算跳过剩余步骤
- 响应新增 `token_budget` 字段
- 新增 skill: `token-budget-control.md`

### Module 3: Runtime Guardrails (已完成)
- 创建 `backend/services/guardrails.py`
- `AnalysisGuardrails` 数据类：max_steps(6), max_sql_queries(8), max_consecutive_failures(2), max_total_time(120s), max_step_time(30s), max_recursion_depth(2), require_minimum_success(1)
- `GuardrailViolation` 异常类，携带 rule + message + context
- `AnalysisGuard` 类：check_before_step(), record_step_result(), check_after_all(), to_dict()
- 预设 STRICT 模式：max_steps=4, max_sql=5, consecutive_fail=1, timeout=60s
- 循环依赖检测：`_chain_depth()` 追踪 depends_on 链
- `run_autonomous_analysis` 集成：每步前检查 guardrail，违规时跳过或停止
- `run_autonomous_analysis_stream` 同步集成
- 响应新增 `guardrails` 和 `guardrail_violations` 字段
- `MultiAnalyzeRequest` 新增 optional `guardrails: dict` 参数
- 新增 skill: `runtime-guardrails.md`

### Module 4: Analysis Trace System (已完成)
- 创建 `backend/services/trace.py`
- `TraceEvent` 数据类：timestamp, operation, phase, prompt_name, input/output tokens, latency, status, error, sql, step
- `AnalysisTrace` 数据类：trace_id, question, table, mode, events, plan, guardrail_violations
- `TraceRecorder` 类：record_llm_call(), record_guardrail_violation(), set_plan(), finish(), to_dict(), save_to_file()
- `_call_llm` / `_call_llm_stream` 新增 trace/phase/prompt_name/step 参数，成功/失败均记录
- 所有 public 函数接受 optional trace 参数并传递至 _call_llm
- `run_autonomous_analysis` 创建 TraceRecorder，规划/步骤/摘要阶段全部记录
- `run_autonomous_analysis_stream` 同步集成
- 响应新增 `trace` 字段：trace_id, total_llm_calls, total_input/output tokens, events
- guardrail 违规记录到 trace
- 新增 skill: `analysis-trace.md`

### Module 5: AI Evaluation Harness (已完成)
- 创建 `tests/ai/` 目录，7 个文件
- `GoldenQuestion` 数据类：id, question, table, expected/not_expected patterns, columns, row range, category, hallucination_check
- 15 个 golden questions 覆盖 8 类：basic(2), aggregation(3), sorting(2), filtering(2), window(1), subquery(1), hallucination(2), edge_case(2)
- `eval_sql.py` 评估逻辑：pattern matching, column check, row count check, hallucination detection
- `eval_hallucination.py` 幻觉检测：正则提取 SQL 中引用的表/列名，与已知 schema 对比
- `runner.py` 评估运行器：run_evaluation() 返回 EvalSummary（pass_rate, hallucination_count, avg_latency, by_category）
- `conftest.py` pytest fixtures：ai_available, generate_fn, execute_fn
- `test_golden_questions.py` parametrized 测试：每个 question 独立 case + 汇总 pass rate 检查
- 新增 skill: `ai-evaluation-harness.md`
- 旧: 8 个 prompt 散落在 2 个 service 文件，无 contract，无结构化 builder
- 新: 8 个 prompt 集中在 `backend/prompts/`，每个有 contract + builder

### 验证
- 后端导入: Pass
- TypeScript: Pass
- Next.js 构建: Pass

## v0.5.3 — Insight Ranking, Semantic Metric Layer, Analysis Planning & Multi-step Executor (2026-05-24)

### Insight Ranking (Module 3)
- `INSIGHTS_SYSTEM` enhanced: each insight now includes `confidence` (0-1), `severity`, `impact`, `category`
- Backward compatible: frontend handles both `string` and `{text, confidence, severity}` formats
- Confidence badges and severity indicators in UI

### Semantic Metric Layer (Module 4)
- `SEMANTICS_SYSTEM` enhanced: columns now include `is_kpi`, `is_time_column`, `is_entity_id`, `aggregation_hint`
- `ColumnSemantics` interface updated with `is_metric`, `is_dimension`, `is_measure`
- KPI/time/entity badges displayed in semantic roles table

### Analysis Planning Engine (Module 1)
- New `ANALYSIS_PLAN_SYSTEM` prompt — AI breaks complex questions into 3-6 structured steps
- `generate_analysis_plan()` — returns plan with `step`, `purpose`, `sql_goal`, `depends_on`
- New `POST /ai/plan` endpoint
- Token budget: ~500 in / ≤1024 out

### Multi-step Analysis Executor (Module 2)
- `run_autonomous_analysis()` — plan → generate SQL per step → execute → executive summary
- `run_autonomous_analysis_stream()` — streaming variant yielding plan/step_start/step_result/summary/done events
- New `POST /ai/analyze-multi` and `POST /ai/analyze-multi/stream` endpoints
- Dependency resolution: dependent steps receive follow-up context from previous step results
- Executive summary generated from step outcomes

### Progressive Streaming (Module 5)
- SSE streaming for autonomous analysis: plan → step_start → step_result → summary → done
- Frontend: `streamAiAnalyzeMulti()` with live plan display and progressive step rendering
- Real-time step results table with row counts and data preview

### Validation
- Frontend build: Pass
- TypeScript: Pass
- Backend: Pass

## v0.5.2 — AI Locale, Semantic Understanding & Smart Questions (2026-05-24)

### AI Locale (语言一致性)
- All AI request models + API calls now accept `language` parameter
- `_apply_language()` appends language instruction to system prompts
- Frontend passes `i18n.language` to all AI endpoints
- Supports `en`, `zh`, and arbitrary language codes

### Semantic Dataset Understanding
- New `POST /ai/semantics` endpoint
- `generate_semantics()` — AI identifies column roles (identifier/metric/dimension/datetime/text), business meanings, detected metrics/dimensions
- Token budget: ~450 in / ≤1024 out
- Integrated in `full-analysis` mode — semantic section with summary, metrics, dimensions, column roles table
- Column count truncated to 20 for token control

### Smart Suggested Questions
- New `POST /ai/suggest-questions` endpoint
- `suggest_questions()` — AI suggests 5 analytical questions (overview/comparison/trend/breakdown/anomaly)
- Token budget: ~450 in / ≤512 out
- Clickable question list in analysis panel — click to populate follow-up input
- Follow-up input now available in both `explain` and `full-analysis` modes

### Validation
- Frontend build: Pass
- Frontend tests: 117/117 Pass
- Backend tests: 161/161 Pass

## v0.5.1 — AI Streaming, Session Memory, Follow-up Analysis & Chart Rendering (2026-05-24)

### AI Streaming Response (SSE)
- Backend: `_call_llm_stream` — Anthropic SDK streaming via `client.messages.stream()`
- Backend: `POST /ai/explain/stream`, `POST /ai/insights/stream` — SSE endpoints
- Frontend: `consumeSseStream`, `streamAiExplain`, `streamAiInsights` — generic SSE consumer
- AI Analysis Panel: live streaming render with pulsing indicator

### AI Session Memory
- New store: `ai-session-store.ts` — conversation turns (max 20), context metadata
- Fields: `turns`, `activeTable`, `lastColumns`, `lastRowCount`, `lastSql`, `lastInsightSummary`
- Backend: `ExplainRequest.conversation_history` — bounded to 6 turns in prompt
- 11 store tests (initial state, turns, MAX_TURNS cap, context, clear, lastSql/lastInsightSummary)

### Result-aware Follow-up Analysis
- Backend: `build_follow_up_context()` — structured context injection (schema + 5 sample rows + previous SQL + insight summary)
- Backend: `FollowUpContext` Pydantic model, `AIQueryRequest.follow_up_context`
- Token budget: ~220-500 tokens (vs ~50,000+ for full result injection)
- Frontend: follow-up input in AI Analysis Panel → `aiQuery(execute: false)` → user confirms → execute
- Fallback: empty context degrades to normal query flow

### Chart Rendering (Recharts)
- New component: `ai-chart.tsx` — ChartSpec → Recharts (bar/line/pie/scatter)
- AI-driven: `aiChartSuggest` auto-called after explain → charts embedded in analysis panel
- Max 2 charts, non-blocking fallback on failure
- No manual chart type selection — AI decides

### Validation
- Frontend build: Pass
- Frontend tests: 117/117 Pass
- Backend tests: 161/161 Pass
- TypeScript: Pass

## v0.3.12 — Maintainability & Governance (2026-05-23)

### Repository Cleanup
- Archived legacy `frontend/` (Streamlit) to `docs/archive/legacy_frontend/`
- Removed stale `backend/data/enterprise.duckdb`
- Created `scripts/start-dev.sh` (start backend + frontend)
- Created `scripts/backup-duckdb.py` (backup/restore/list)

### Coverage Audit
- Ran real frontend coverage: 38.25% lines, 23.88% branches
- Ran real backend coverage: 47%
- Generated `coverage-improvement-plan.md` with prioritized phases

### Skills
- Created `skills/active/virtual-scroll-debugging.md`
- Created `skills/active/performance-audit.md`
- Updated `SKILL_REGISTRY.md` with new skills

### Bug Fixes (Auto Bug Hunt)
- Fix Monaco stale `onExecute` closure — Ctrl+Enter now executes current SQL
- Fix AbortController cleanup on unmount — no more stale toasts
- Fix `getVirtualItems()` dependency array — eliminated re-render loop risk
- Fix `handleLoadSaved` stale activeTab — loads SQL into correct tab
- Add `hasMore`/`totalRows`/`truncated` to query API response
- Add truncation warning in query stats bar

### API Changes
- `POST /api/query` response now includes `totalRows`, `hasMore`, `truncated`

### Validation
- Frontend build: Pass
- Frontend tests: 21/21 Pass
- Backend tests: 28/28 Pass

## v0.3.11 — Repository Architecture Governance (2026-05-23)

### Governance
- Created `FILE_SYSTEM_RULES.md` — repository structure governance
- Created `SKILL_LIFECYCLE.md` — skill state management
- Created `REPOSITORY_HEALTH.md` — automated health audit (78/100)
- Removed debug artifacts, empty stubs, cache directories
- Updated `.gitignore` with `.pytest_cache/`, `temp/`

## v0.3.10 — Enterprise Maintenance System (2026-05-23)

### Bug Fixes
- Fix SQL execution failure (DuckDB lock — killed stale process)
- Fix blank page issue (corrupted `.next` build cache)
- Fix Cancel button (`queryId` moved from useState to useRef)
- Fix `handleExecute` stale closure (use `getState()` directly)

### Testing
- Added 12 Playwright E2E tests
- Fixed all existing test suites

### Governance
- Created docs architecture structure
- Added workflow governance documentation

## v0.3.9 — Stability & Consistency Pass (2026-05-23)

### Bug Fixes
- Fix SQL query limit from 500 → 10,000 rows (frontend + backend)
- Fix DataTable `contain: strict` → `contain: layout style` (virtualization scroll fix)

### Governance
- Restructure docs/ directory (architecture/governance/testing/reports/archive)
- Create docs/README.md documentation index
- Update KNOWN_ISSUES.md with new/closed issues
- Update CLAUDE.md version

### Validation
- Frontend build: Pass
- Frontend tests: 21/21 Pass
- Backend tests: 28/28 Pass
- API query limit: Verified (5000 rows returned with default 10K limit)

## v0.3.8 — AI Engineering Governance (2026-05-23)

### 新增
- CLAUDE.md 长期协同开发规则
- SESSION_SUMMARY_TEMPLATE.md 上下文恢复模板
- KNOWN_ISSUES.md 问题跟踪
- TESTING_STRATEGY.md 测试策略
- SKILL_REGISTRY.md 技能注册
- ERROR_ANALYSIS.md 错误分析体系
- AB_TESTING.md A/B 测试框架
- Vitest 前端测试 (21 tests)
- pytest 后端测试 (28 tests)

### 文档治理
- 更新 docs/项目架构说明.md (Streamlit → React/FastAPI/DuckDB)
- 更新 docs/开发路线图.md
- 更新 docs/版本记录.md
- 更新 testExcel/README.md

## v0.3.7 — Enterprise Query Experience (2026-05-23)

### 新增
- Monaco Editor SQL 自动补全 (关键字/表名/列名/DuckDB函数)
- Query Tabs 多标签页系统 (Zustand persist)
- Saved Queries 保存/收藏/管理
- Query Explain 可视化 (DuckDB EXPLAIN)
- Query Cancel (AbortController + 后端取消)
- Export System (CSV/JSON/Excel 导出)
- SQL Formatting (sql-formatter)
- Keyboard Shortcuts (Ctrl+Enter/S/L//)
- Query Statistics (行数/耗时/列数)
- Query History 增强 (搜索/筛选/删除/清空/重新运行/导出)

### 技术栈
- @monaco-editor/react, sql-formatter, xlsx
- 后端新增 5 个 API 端点 (explain, cancel, export, schema, history)
- 3 个新 Zustand stores, 3 个新组件

## v0.3.6 — Workspace Polish (2026-05-22)

### 新增
- Workspace 持久化 (Zustand persist)
- Query Cache
- Virtual Table 性能优化
- Query Runtime 监控

## v0.3.5 — Data Quality (2026-05-22)

### 新增
- 数据质量分析引擎
- 缺失值/异常值/重复行检测
- 质量评分系统 (完整性/一致性/有效性/唯一性)
- Field Health 字段健康度

## v0.3.4 — DuckDB Management Layer (2026-05-22)

### 新增
- 表 CRUD 操作
- Schema 检测与类型映射
- CSV 导出 (StreamingResponse)
- 表重命名

## v0.3.3 — React Workspace Foundation (2026-05-21)

### 新增
- SQL Workspace (textarea + 执行按钮)
- File Upload (CSV/Excel 拖拽上传)
- Data Preview (虚拟化表格)
- Table Management (表列表/删除)
- Query History

## v0.3.2 — Workspace Refactor (2026-05-21)

### 新增
- 面板系统重构 (react-resizable-panels)
- i18n 国际化 (en/zh)
- Zustand 状态管理 (workspace-store)

## v0.3.1 — React Workspace Foundation (2026-05-21)

### 新增
- Next.js 15 项目搭建
- React 19 + TypeScript
- TailwindCSS 暗色主题
- 基础 3-panel 布局
- FastAPI 后端代理

## v0.1 — 项目初始化 (2026-05-20)

### 新增
- 项目基础结构
- 目录规划
- Python 依赖配置
- 初始技术选型 (后已变更为 React/FastAPI/DuckDB)

# Bug Hunt Report — v0.3.12

> Generated: 2026-05-23

## Summary

10 issues found: 1 High, 5 Medium, 4 Low.
Fixed 6 issues (1 High + 5 Medium). 4 Low deferred.

## Issues Found and Fixed

### FIXED (6)

| # | File | Issue | Severity | Fix |
|---|------|-------|----------|-----|
| 1 | `monaco-sql-editor.tsx:234` | Stale `onExecute` closure — Ctrl+Enter executes stale SQL | Medium | Use `onExecuteRef` instead of closure capture |
| 3 | `sql-workspace-panel.tsx:33` | AbortController not cleaned up on unmount — toasts fire after navigation | Medium | Added `useEffect` cleanup that aborts on unmount |
| 4 | `data-table.tsx:100-101` | `getVirtualItems()` in dependency array causes re-render loop risk | Medium | Removed from deps; use `rows.length` as trigger |
| 6 | `sql-workspace-store.ts` | `hasMore`/`truncated`/`totalRows` not in types — silent data truncation | Medium | Added fields to both store and API interfaces |
| 7 | `sql-workspace-panel.tsx:497` | Truncation not shown in stats bar | Medium | Added yellow warning when `truncated` is true |
| 10 | `sql-workspace-panel.tsx:184` | `handleLoadSaved` captures stale `activeTab` — loads SQL into wrong tab | Low | Read `getActiveTab()` from store state directly |

### DEFERRED (4)

| # | File | Issue | Severity | Reason |
|---|------|-------|----------|--------|
| 2 | `sql-workspace-panel.tsx:60` | Non-atomic `isExecuting` guard (works by accident) | Low | Zustand synchronous updates make this safe; refactor risk > benefit |
| 5 | `data-table.tsx:158` | `contain: "layout style"` may interfere with sticky headers | Low | Browser-dependent; not reproducible on Chrome |
| 6b | `sql-workspace-panel.tsx:39,74` | `queryId` only set after response — server-side cancel unreachable during execution | High | Architectural: server generates queryId, can't know it until response. Client-side abort (AbortController) is the working mechanism. Needs server refactor (pre-assign ID) — deferred to v0.4.x |
| 8 | `monaco-sql-editor.tsx` | No self-hosted Monaco worker config; depends on external CDN | Low | Works for dev/local; deployment concern only |

## Verification

- Frontend build: Pass
- Frontend tests: 21/21 Pass
- Backend tests: 28/28 Pass
- TypeScript: No type errors
